fixtures:
  repositories:
    "stdlib": "git://github.com/puppetlabs/puppetlabs-stdlib.git"
    "concat": "git://github.com/puppetlabs/puppetlabs-concat.git"
  symlinks:
    "corp104_cloudwatch_agent": "#{source_dir}"
